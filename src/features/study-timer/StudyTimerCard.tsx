import{formatDuration,useStudyTimer}from'./useStudyTimer'
export function StudyTimerCard(){const timer=useStudyTimer();return <section className="card study-card"><div className="card-head"><h2>Today's Study</h2><span className="meta">{timer.isRunning?'Running':'Paused'}</span></div><div className="timer-value tabular">{formatDuration(timer.seconds)}</div><div className="timer-controls"><button className="button primary" onClick={timer.start} disabled={timer.isRunning}>Start</button><button className="button" onClick={timer.pause} disabled={!timer.isRunning}>Pause</button><button className="button" onClick={()=>timer.addMinutes(10)}>+ 10m</button></div></section>}
