import type{Task}from'../../tasks/types';import type{CalendarEvent}from'../types';import{addDays,isoDate,sameDate}from'../date'
type Selection={kind:'task';value:Task}|{kind:'event';value:CalendarEvent}
export function CalendarMonth({start,month,tasks,events,onCreate,onSelect}:{start:Date;month:number;tasks:Task[];events:CalendarEvent[];onCreate:(date:Date)=>void;onSelect:(selection:Selection)=>void}){const today=new Date(),days=Array.from({length:42},(_,i)=>addDays(start,i));return <div className="month-grid">{days.map(date=>{const key=isoDate(date),dayTasks=tasks.filter(t=>t.dueDate===key),dayEvents=events.filter(e=>e.startDate<=key&&e.endDate>=key),items=[...dayEvents.map(value=>({kind:'event'as const,value})),...dayTasks.map(value=>({kind:'task'as const,value}))];return <div key={key} className={`month-day${date.getMonth()!==month?' outside':''}${sameDate(date,today)?' today':''}`} onClick={()=>onCreate(date)} role="button" tabIndex={0} onKeyDown={e=>{if(e.key==='Enter')onCreate(date)}}><div className="month-date"><span>{date.getDate()===1?`${date.getMonth()+1}월 ${date.getDate()}일`:date.getDate()}</span></div><div className="month-items">{items.slice(0,5).map(item=>{const title=item.value.title,color=item.value.subject?.color??(item.kind==='task'?'#0a84ff':'#bf5af2');const time=item.kind==='event'&&!item.value.allDay?item.value.startTime?.slice(0,5):'';return <button key={`${item.kind}-${item.value.id}`} className={`month-entry ${item.kind}${'isMajor'in item.value&&item.value.isMajor?' major':''}`} style={{'--entry-color':color}as React.CSSProperties} onClick={e=>{e.stopPropagation();onSelect(item)}}><i/>{time&&<small>{time}</small>}<span>{title}</span></button>})}{items.length>5&&<span className="month-more">{items.length-5}개 더 보기</span>}</div></div>})}</div>}
