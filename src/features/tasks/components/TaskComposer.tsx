import{useEffect,useState,type FormEvent}from'react';import{createTask,updateTask}from'../api/tasks';import{taskCategoryLabels,type Subject,type Task,type TaskCategory}from'../types'
export function TaskComposer({subjects,editing,onDone}:{subjects:Subject[];editing:Task|null;onDone:()=>void}){const[title,setTitle]=useState(''),[category,setCategory]=useState<TaskCategory>('study'),[subjectId,setSubjectId]=useState(''),[dueDate,setDueDate]=useState(''),[saving,setSaving]=useState(false),[error,setError]=useState('');useEffect(()=>{setTitle(editing?.title??'');setCategory(editing?.category??'study');setSubjectId(editing?.subjectId??'');setDueDate(editing?.dueDate??'')},[editing]);async function submit(event:FormEvent){event.preventDefault();setSaving(true);setError('');const input={title,category,subjectId:category==='study'&&subjectId?subjectId:null,dueDate:dueDate||null};try{if(editing)await updateTask(editing.id,input);else await createTask(input);setTitle('');setDueDate('');if(!editing)setSubjectId('');onDone()}catch{setError('Could not save this task. Check for duplicate or invalid values.')}finally{setSaving(false)}}return <form className="task-composer" onSubmit={submit}><div className="composer-title"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="What needs to be done?" maxLength={240} required/><button className="button primary" disabled={saving}>{saving?'Saving…':editing?'Save':'Add task'}</button></div><div className="composer-fields"><select value={category} onChange={e=>setCategory(e.target.value as TaskCategory)}>{Object.entries(taskCategoryLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select>{category==='study'&&<select value={subjectId} onChange={e=>setSubjectId(e.target.value)}><option value="">No subject</option>{subjects.map(subject=><option key={subject.id} value={subject.id}>{subject.name}</option>)}</select>}<label>Due <input type="date" value={dueDate} onChange={e=>setDueDate(e.target.value)}/></label>{editing&&<button type="button" className="text-button" onClick={onDone}>Cancel</button>}</div>{error&&<p className="form-error">{error}</p>}</form>}
