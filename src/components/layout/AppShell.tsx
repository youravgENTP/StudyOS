import{BarChart3,CalendarDays,CalendarRange,CheckSquare2,ChevronsLeft,ChevronsRight,Coffee,Gauge,ListChecks,Quote,Repeat2,Settings}from'lucide-react'
import{useState}from'react'
import{NavLink,Outlet,useLocation}from'react-router-dom'
import'./app-shell-calendar.css'
const nav=[['Dashboard','/',Gauge],['Calendar','/calendar',CalendarDays],['Schedules','/schedules',CalendarRange],['Tasks','/tasks',CheckSquare2],['Routine','/routine',ListChecks],['Habits','/habits',Repeat2],['Caffeine','/caffeine',Coffee],['Quotes','/quotes',Quote],['Stats','/stats',BarChart3],['Settings','/settings',Settings]]as const
function Navigation({items=nav}:{items?:readonly(typeof nav[number])[]}){return <>{items.map(([label,to,Icon])=><NavLink key={to} to={to} end={to==='/' } className={({isActive})=>`nav-link${isActive?' active':''}`}><Icon size={18}/><span>{label}</span></NavLink>)}</>}
export type AppShellContext={calendarToolsOpen:boolean;setCalendarToolsOpen:(open:boolean)=>void}
export function AppShell(){const location=useLocation();const onCalendar=location.pathname==='/calendar';const[calendarToolsOpen,setCalendarToolsOpen]=useState(true);const[mainSidebarCollapsed,setMainSidebarCollapsed]=useState(false);const showMainSidebar=!onCalendar||!calendarToolsOpen;const collapsed=showMainSidebar&&mainSidebarCollapsed;return <div className={`shell${showMainSidebar?'':' calendar-tools-active'}${collapsed?' sidebar-collapsed':''}`}>{showMainSidebar&&<aside className="sidebar"><div className="sidebar-head"><div className="brand"><span className="brand-mark">S</span>{!collapsed&&<span>StudyOS</span>}</div><div className="sidebar-directions">{collapsed?<button onClick={()=>setMainSidebarCollapsed(false)} aria-label="Expand main sidebar"><ChevronsRight/></button>:<><button onClick={()=>setMainSidebarCollapsed(true)} aria-label="Collapse main sidebar"><ChevronsLeft/></button>{onCalendar&&<button onClick={()=>setCalendarToolsOpen(true)} aria-label="Open Calendar sidebar"><ChevronsRight/></button>}</>}</div></div>{!collapsed&&<nav className="nav"><Navigation/></nav>}</aside>}<main className="main"><Outlet context={{calendarToolsOpen,setCalendarToolsOpen} satisfies AppShellContext}/></main><nav className="mobile-nav"><Navigation items={nav.slice(0,5)}/></nav></div>}
