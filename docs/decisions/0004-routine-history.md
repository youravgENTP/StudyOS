# ADR 0004: Routine templates create stable daily instances

**Status:** Accepted

Weekday templates describe future operating protocols. Each day creates an instance with copied or snapshotted items so later template edits cannot rewrite history.
