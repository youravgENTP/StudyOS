# StudyOS architecture

This is the human-readable mental map of StudyOS. Feature documents describe ownership, database documents describe durable records, and concise ADRs preserve consequential decisions. `relations.json` is the graph consumed by the architecture viewer.

When an important feature, service, hook, table, or data flow changes, update its Markdown document and graph relationships.
